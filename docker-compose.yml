version: "3.3"
services:
  web:
    restart: unless-stopped
    build: .
    pull_policy: build
    # ports:
    #   - 8000:80
    volumes:
      - ./app:/app

    labels:
      - "traefik.enable=true"

      - "traefik.http.routers.wa-bogdanov-http.rule=Host(`bogdanov.wa.dtcloud.cz`)"
      - "traefik.http.routers.wa-bogdanov-http.entrypoints=web"
      - "traefik.http.routers.wa-bogdanov-http.middlewares=wa-bogdanov-redirect-https@docker"

      - "traefik.http.middlewares.wa-bogdanov-redirect-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.wa-bogdanov-redirect-https.redirectscheme.permanent=true"

      - "traefik.http.routers.wa-bogdanov-https.rule=Host(`bogdanov.wa.dtcloud.cz`)"
      - "traefik.http.routers.wa-bogdanov-https.entrypoints=websecure"
      - "traefik.http.routers.wa-bogdanov-https.tls=true"
      - "traefik.http.routers.wa-bogdanov-https.tls.certresolver=letsencrypt"

  static:
    image: nginx:alpine
    restart: unless-stopped

    volumes:
      - ./app/static:/usr/share/nginx/html/static:ro

    labels:
      - "traefik.enable=true"

      - "traefik.http.routers.wa-bogdanov-static-http.rule=Host(`bogdanov.wa.dtcloud.cz`) && PathPrefix(`/static`)"
      - "traefik.http.routers.wa-bogdanov-static-http.entrypoints=web"
      - "traefik.http.routers.wa-bogdanov-static-http.middlewares=wa-bogdanov-static-redirect-https@docker"

      - "traefik.http.middlewares.wa-bogdanov-static-redirect-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.wa-bogdanov-static-redirect-https.redirectscheme.permanent=true"

      - "traefik.http.routers.wa-bogdanov-static-https.rule=Host(`bogdanov.wa.dtcloud.cz`) && PathPrefix(`/static`)"
      - "traefik.http.routers.wa-bogdanov-static-https.entrypoints=websecure"
      - "traefik.http.routers.wa-bogdanov-static-https.tls=true"
      - "traefik.http.routers.wa-bogdanov-static-https.tls.certresolver=letsencrypt"